#include <iostream>
#include <cmath>
using namespace std;




int main(){
    unsigned long long int n, m;
    cin>>n>>m;
    cout<<(unsigned long long int)sqrt(n)*(unsigned long long int)sqrt(m);

    return 0;
}